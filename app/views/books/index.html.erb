<%= title pl(:book) %>

<div id="top_links">
  <%= link_to new(:book), new_book_path if can? :new, Book %>
</div>

<%= will_paginate @books, :class => "digg_pagination" %>

<div id="books">
  <%= render @books %>
</div>

<div id="bottom_links">
  <%= link_to new(:book), new_book_path if can? :new, Book %>
</div>
