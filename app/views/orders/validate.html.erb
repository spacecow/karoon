<%= title confirm(:order) %>

<%= render 'orders/line_items', :line_items => @order.line_items, :model => @order %>

<%= render @order %>

<%= form_for @order, :url => confirm_order_path(@order), :method => :put, :html=>{:id => 'confirm_order'} do |f| %>
  <%= f.submit cancel(:order), :name => :cancel_button, :class => :submit, :confirm => sure? %>
  <%= f.submit edit(:order), :name => :edit_button, :class => :submit %>
  <%= f.submit confirm(:order), :class => :submit %>
<% end %>
